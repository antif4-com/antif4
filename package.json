{
  "name": "antif4",
  "version": "0.0.1",
  "description": "js for antif4.com",
  "main": "index.js",
  "scripts": {
    "server": "hugo server -w",
    "build": "./update_css.sh && hugo",
    "deploy": "hugo && ./build_deploy.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "eian@antif4.com",
  "license": "ISC",
  "dependencies": {
    "@webtui/css": "^0.1.4",
    "@webtui/theme-catppuccin": "^0.0.3"
  }
}
